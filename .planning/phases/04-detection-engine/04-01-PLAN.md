---
phase: 04-detection-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpg/pipeline/detector.py
  - pnpg/config.py
  - config.yaml
  - data/tor-exit-nodes.txt
  - tests/test_pipeline/test_detector.py
  - tests/conftest.py
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04, DET-05, DET-06, DET-07, DET-08, DET-10]

must_haves:
  truths:
    - "An event with dst_hostname == dst_ip (no DNS) and a public IP produces a DET-01 WARNING alert"
    - "A process with >100 connections in 60 seconds produces a DET-02 HIGH alert"
    - "An unusual port from an unknown process produces a DET-03 alert; from a known process at normal rate does NOT"
    - "An event with process_name == 'unknown process' produces a DET-04 ALERT"
    - "An event with threat_intel.is_blocklisted == True produces a DET-05 CRITICAL alert"
    - "An event with dst_ip in the TOR exit node set produces a DET-06 HIGH alert"
    - "The first connection from a process to a new dst_ip produces a DET-07 LOW alert; repeat does NOT"
    - "All threshold values are read from config dict, never hardcoded in rule functions"
    - "Every alert dict contains all 12 DET-10 fields"
  artifacts:
    - path: "pnpg/pipeline/detector.py"
      provides: "DetectorState dataclass, 7 rule functions, _make_alert factory, _process_key helper, _update_connection_rate, _is_allowlisted stub, load_tor_exit_nodes"
      exports: ["DetectorState", "detect_event", "load_tor_exit_nodes"]
    - path: "tests/test_pipeline/test_detector.py"
      provides: "Unit tests for DET-01 through DET-08, DET-10 alert object validation"
    - path: "pnpg/config.py"
      provides: "New DEFAULT_CONFIG keys: connection_rate_threshold_per_min, tor_exit_list_path, known_processes"
      contains: "connection_rate_threshold_per_min"
  key_links:
    - from: "pnpg/pipeline/detector.py"
      to: "pnpg/config.py"
      via: "config dict keys: connection_rate_threshold_per_min, port_allowlist, tor_exit_list_path, known_processes"
      pattern: "config\\[.connection_rate_threshold_per_min.\\]"
    - from: "tests/test_pipeline/test_detector.py"
      to: "pnpg/pipeline/detector.py"
      via: "import DetectorState, detect_event"
      pattern: "from pnpg.pipeline.detector import"
---

<objective>
Build the detection engine core: DetectorState dataclass, 7 detection rule functions (DET-01 through DET-07), the _make_alert factory (DET-10), TOR exit node loader (DET-06), sliding window rate counter (DET-02), config constants (DET-08), and comprehensive tests.

Purpose: This is the first half of the detection engine. It creates all rule logic and the alert object schema. Plan 02 adds rate limiting, allowlist, suppression, and wires into the pipeline.
Output: pnpg/pipeline/detector.py with DetectorState + 7 rules + alert factory; tests/test_pipeline/test_detector.py with one test per rule + alert schema test; updated config.py and config.yaml with Phase 4 keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-detection-engine/04-RESEARCH.md
@.planning/phases/04-detection-engine/04-VALIDATION.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From pnpg/config.py (DEFAULT_CONFIG — keys to ADD to):
```python
DEFAULT_CONFIG: dict = {
    "queue_size": 500,
    "poll_interval_ms": 200,
    "alert_rate_limit_per_sec": 1,
    "port_allowlist": [80, 443, 53, 123, 5353, 8080, 8443],
    "log_rotation_size_mb": 50,
    "debug_mode": False,
    "interface": None,
    "connection_rate_threshold": 50,
    "dns_cache_size": 1000,
    "dns_cache_ttl_sec": 300,
    "proc_cache_ttl_sec": 2,
    "log_dir": "logs",
    "geoip_country_db": "data/GeoLite2-Country.mmdb",
    "geoip_asn_db": "data/GeoLite2-ASN.mmdb",
    "blocklist_path": "data/blocklist.txt",
}
```

From pnpg/pipeline/threat_intel.py (check_threat_intel output format):
```python
def check_threat_intel(event: dict) -> dict:
    # Returns: {**event, "threat_intel": {"is_blocklisted": bool, "source": str|None}}
```

From pnpg/pipeline/process_mapper.py (sentinel value):
```python
# "unknown process" is the exact sentinel string for unattributed processes
# PID is -1 for unknown processes
```

From REQUIREMENTS.md Alert Object (all 12 DET-10 fields):
```python
{
    "alert_id": "uuid",
    "timestamp": "ISO8601",
    "severity": "HIGH",
    "rule_id": "DET-02",
    "reason": "description string",
    "confidence": 0.91,
    "process_name": "chrome",
    "pid": 4821,
    "dst_ip": "93.184.216.34",
    "dst_hostname": "example.com",
    "recommended_action": "REVIEW",
    "suppressed": False,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DetectorState, config keys, TOR loader, alert factory, and test scaffolding</name>
  <files>pnpg/pipeline/detector.py, pnpg/config.py, config.yaml, data/tor-exit-nodes.txt, tests/test_pipeline/test_detector.py, tests/conftest.py</files>
  <read_first>
    - pnpg/config.py (current DEFAULT_CONFIG to extend)
    - config.yaml (current config keys documented)
    - pnpg/pipeline/threat_intel.py (load_blocklist pattern to mirror for load_tor_exit_nodes)
    - tests/conftest.py (existing fixtures to extend)
    - tests/test_pipeline/test_worker.py (existing test patterns)
  </read_first>
  <behavior>
    - Test: DetectorState() creates instance with empty connection_timestamps dict, empty first_seen dict, empty rate_limiter dict, empty frozenset tor_exit_nodes, empty allowlist list, empty suppressed_alert_ids set, empty suppressed_rules set
    - Test: _make_alert() returns dict with all 12 keys: alert_id, timestamp, severity, rule_id, reason, confidence, process_name, pid, dst_ip, dst_hostname, recommended_action, suppressed
    - Test: _make_alert() alert_id is a valid UUID4 string
    - Test: _make_alert() timestamp is ISO8601 with timezone
    - Test: load_tor_exit_nodes("data/tor-exit-nodes.txt") returns frozenset with IPs from file
    - Test: load_tor_exit_nodes("nonexistent.txt") returns empty frozenset without raising
    - Test: _process_key returns process_name for known processes, "unknown_process_{pid}" for "unknown process" sentinel
    - Test: _update_connection_rate returns count of events within 60-second window
    - Test: DEFAULT_CONFIG contains "connection_rate_threshold_per_min" key with value 100
    - Test: DEFAULT_CONFIG contains "tor_exit_list_path" key with value "data/tor-exit-nodes.txt"
    - Test: DEFAULT_CONFIG contains "known_processes" key with value list
  </behavior>
  <action>
    1. Update pnpg/config.py DEFAULT_CONFIG — add these 3 new keys after the existing "blocklist_path" key:
       - "connection_rate_threshold_per_min": 100
       - "tor_exit_list_path": "data/tor-exit-nodes.txt"
       - "known_processes": ["chrome.exe", "firefox.exe", "msedge.exe", "explorer.exe", "svchost.exe", "System", "python.exe", "node.exe", "code.exe"]

    2. Update config.yaml — add commented-out documentation for the 3 new keys after existing entries:
       ```
       # connection_rate_threshold_per_min: 100  # Max connections per minute per process (DET-02)
       # tor_exit_list_path: data/tor-exit-nodes.txt  # Path to TOR exit node IP list (DET-06)
       # known_processes: [chrome.exe, firefox.exe, msedge.exe, explorer.exe, svchost.exe, System, python.exe, node.exe, code.exe]  # Known-good process names (DET-03)
       ```

    3. Create data/tor-exit-nodes.txt — a placeholder file with a comment header and 3 example IPs:
       ```
       # TOR exit node IPs - download from https://check.torproject.org/torbulkexitlist
       # Last updated: placeholder
       185.220.101.1
       185.220.101.2
       185.220.101.3
       ```

    4. Create pnpg/pipeline/detector.py with:
       - Imports: uuid, time, logging, datetime (timezone, datetime), dataclasses (dataclass, field), ipaddress
       - logger = logging.getLogger(__name__)
       - @dataclass class DetectorState with 7 fields:
         - connection_timestamps: dict = field(default_factory=dict)  # {process_key: list[float]}
         - first_seen: dict = field(default_factory=dict)  # {process_key: set[str]}
         - rate_limiter: dict = field(default_factory=dict)  # {(rule_id, process_key): float}
         - tor_exit_nodes: frozenset = field(default_factory=frozenset)
         - allowlist: list = field(default_factory=list)  # list[dict]
         - suppressed_alert_ids: set = field(default_factory=set)  # set[str]
         - suppressed_rules: set = field(default_factory=set)  # set[tuple[str, str|None]]
       - def load_tor_exit_nodes(path: str) -> frozenset: mirrors threat_intel.load_blocklist pattern — read file, skip comments/blanks, return frozenset of stripped lines; FileNotFoundError returns empty frozenset with WARNING log
       - def _process_key(event: dict) -> str: returns event["process_name"] for known processes, f"unknown_process_{pid}" when process_name == "unknown process"
       - def _update_connection_rate(process_key: str, state: DetectorState, config: dict) -> int: appends time.monotonic() to state.connection_timestamps[process_key], evicts entries older than 60 seconds, returns len of remaining list
       - def _make_alert(event, rule_id, severity, reason, confidence, recommended_action, suppressed=False) -> dict: returns dict with all 12 DET-10 fields. alert_id=str(uuid.uuid4()), timestamp=datetime.now(timezone.utc).isoformat(), process_name from event default "unknown process", pid from event default -1, dst_ip from event default "", dst_hostname from event default ""
       - def _is_allowlisted(event: dict, state: DetectorState) -> bool: (stub for Plan 01, full implementation in Plan 02) iterates state.allowlist checking expiry (ALLOW-03), process scope (ALLOW-01/02), and destination match (IP or hostname). Returns True if any rule matches.

    5. Update tests/conftest.py — add two fixtures:
       - @pytest.fixture def detector_state(): returns DetectorState()
       - @pytest.fixture def enriched_event(): returns a sample fully-enriched event dict with all fields (process_name="chrome.exe", pid=1234, dst_ip="93.184.216.34", dst_hostname="example.com", dst_port=443, protocol="TCP", src_ip="192.168.1.5", src_port=52341, threat_intel={"is_blocklisted": False, "source": None}, dst_country="US", dst_asn="AS15169", dst_org="Google LLC")

    6. Create tests/test_pipeline/test_detector.py with tests for DetectorState init, _make_alert (12 fields check, UUID validity, ISO8601 timestamp), load_tor_exit_nodes (happy path + missing file), _process_key (known + unknown), _update_connection_rate (counts within window, evicts old), and config key existence.
  </action>
  <verify>
    <automated>cd "C:/Users/alame/Desktop/network Lab project" && python -m pytest tests/test_pipeline/test_detector.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpg/pipeline/detector.py contains "class DetectorState"
    - pnpg/pipeline/detector.py contains "def _make_alert("
    - pnpg/pipeline/detector.py contains "def load_tor_exit_nodes("
    - pnpg/pipeline/detector.py contains "def _process_key("
    - pnpg/pipeline/detector.py contains "def _update_connection_rate("
    - pnpg/pipeline/detector.py contains "def _is_allowlisted("
    - pnpg/config.py contains "connection_rate_threshold_per_min": 100
    - pnpg/config.py contains "tor_exit_list_path": "data/tor-exit-nodes.txt"
    - pnpg/config.py contains "known_processes"
    - config.yaml contains "connection_rate_threshold_per_min"
    - config.yaml contains "tor_exit_list_path"
    - data/tor-exit-nodes.txt exists and contains at least one non-comment line
    - tests/test_pipeline/test_detector.py contains "test_detector_state_init" or equivalent
    - tests/test_pipeline/test_detector.py contains "test_make_alert"
    - tests/conftest.py contains "def detector_state"
    - tests/conftest.py contains "def enriched_event"
    - pytest tests/test_pipeline/test_detector.py -x -q exits 0
  </acceptance_criteria>
  <done>DetectorState dataclass, alert factory, TOR loader, process key helper, sliding window counter, and allowlist checker stub all exist with passing tests. Config keys added to DEFAULT_CONFIG and documented in config.yaml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seven detection rule functions (DET-01 through DET-07) with unit tests</name>
  <files>pnpg/pipeline/detector.py, tests/test_pipeline/test_detector.py</files>
  <read_first>
    - pnpg/pipeline/detector.py (DetectorState, _make_alert, _process_key, _update_connection_rate from Task 1)
    - pnpg/config.py (DEFAULT_CONFIG with new keys)
    - tests/test_pipeline/test_detector.py (existing test scaffold from Task 1)
    - tests/conftest.py (detector_state and enriched_event fixtures)
  </read_first>
  <behavior>
    - Test DET-01: event with dst_hostname == dst_ip (public IP "93.184.216.34") returns DET-01 alert with severity="WARNING"; event with dst_hostname != dst_ip returns None; event with private IP (192.168.1.1) where dst_hostname == dst_ip returns None (private IP skip)
    - Test DET-02: after inserting 101 timestamps within 60s for a process key, rule returns DET-02 alert with severity="HIGH"; at 100 returns None
    - Test DET-03: event with dst_port=9999 (not in port_allowlist) AND process_name="unknown process" returns DET-03 alert; event with dst_port=9999 AND process_name="chrome.exe" at normal rate returns None (false-positive gate)
    - Test DET-04: event with process_name="unknown process" returns DET-04 alert with severity="ALERT"; event with process_name="chrome.exe" returns None
    - Test DET-05: event with threat_intel.is_blocklisted=True returns DET-05 alert with severity="CRITICAL"; threat_intel.is_blocklisted=False returns None
    - Test DET-06: event with dst_ip in state.tor_exit_nodes returns DET-06 alert with severity="HIGH"; dst_ip not in set returns None
    - Test DET-07: first event for process+dst_ip returns DET-07 alert with severity="LOW"; second identical event returns None
    - Test DET-08: all rule functions read thresholds from config dict, none contain hardcoded numeric thresholds (verified by checking rule function source code does not contain literal 100 for rate threshold)
  </behavior>
  <action>
    Add 7 rule functions to pnpg/pipeline/detector.py after the helper functions from Task 1:

    1. def rule_det01_unknown_domain(event, config, state) -> dict | None:
       - Get dst_ip and dst_hostname from event
       - If dst_ip is empty, return None
       - If dst_hostname exists and dst_hostname != dst_ip, return None (DNS resolved OK)
       - Use ipaddress.ip_address(dst_ip).is_private to skip private/loopback IPs — return None if private
       - Otherwise return _make_alert(event, rule_id="DET-01", severity="WARNING", reason=f"No reverse DNS record for {dst_ip}", confidence=0.5, recommended_action="MONITOR")

    2. def rule_det02_rate_spike(event, config, state) -> dict | None:
       - process_key = _process_key(event)
       - count = _update_connection_rate(process_key, state, config)
       - threshold = config.get("connection_rate_threshold_per_min", 100)
       - If count > threshold, return _make_alert(event, rule_id="DET-02", severity="HIGH", reason=f"{event.get('process_name','unknown')} exceeded {threshold} connections/min (current: {count})", confidence=0.85, recommended_action="REVIEW")
       - Else return None
       - IMPORTANT: _update_connection_rate is always called (state update) regardless of whether alert fires

    3. def rule_det03_unusual_port(event, config, state) -> dict | None:
       - dst_port = event.get("dst_port", 0)
       - port_allowlist = config.get("port_allowlist", [80, 443, 53, 123, 5353, 8080, 8443])
       - If dst_port in port_allowlist, return None (safe port)
       - Compound guard: process_name = event.get("process_name", "")
       - known_processes = config.get("known_processes", [])
       - is_unknown = (process_name == "unknown process")
       - process_key = _process_key(event)
       - count = len(state.connection_timestamps.get(process_key, []))
       - rate_threshold = config.get("connection_rate_threshold_per_min", 100)
       - is_rate_spike = (count > rate_threshold)
       - If NOT is_unknown AND NOT is_rate_spike, return None (false-positive gate — known process at normal rate)
       - Return _make_alert(event, rule_id="DET-03", severity="WARNING", reason=f"Unusual port {dst_port} from {'unknown process' if is_unknown else process_name + ' (rate spike)'}", confidence=0.6, recommended_action="INVESTIGATE")

    4. def rule_det04_unknown_process(event, config, state) -> dict | None:
       - If event.get("process_name") == "unknown process", return _make_alert(event, rule_id="DET-04", severity="ALERT", reason=f"Connection from unresolvable process (PID {event.get('pid', -1)}) to {event.get('dst_ip', '')}", confidence=0.7, recommended_action="INVESTIGATE")
       - Else return None

    5. def rule_det05_blocklisted(event, config, state) -> dict | None:
       - threat_intel = event.get("threat_intel", {})
       - If threat_intel.get("is_blocklisted") is True, return _make_alert(event, rule_id="DET-05", severity="CRITICAL", reason=f"Connection to blocklisted destination {event.get('dst_ip','')}/{event.get('dst_hostname','')}", confidence=0.95, recommended_action="BLOCK")
       - Else return None

    6. def rule_det06_tor_exit_node(event, config, state) -> dict | None:
       - dst_ip = event.get("dst_ip", "")
       - If dst_ip and dst_ip in state.tor_exit_nodes, return _make_alert(event, rule_id="DET-06", severity="HIGH", reason=f"Connection to TOR exit node {dst_ip}", confidence=0.9, recommended_action="REVIEW")
       - Else return None

    7. def rule_det07_new_destination(event, config, state) -> dict | None:
       - process_key = _process_key(event)
       - dst_ip = event.get("dst_ip", "")
       - If not dst_ip, return None
       - seen = state.first_seen.setdefault(process_key, set())
       - If dst_ip in seen, return None
       - seen.add(dst_ip)
       - Return _make_alert(event, rule_id="DET-07", severity="LOW", reason=f"First connection from {event.get('process_name','unknown')} to {dst_ip}", confidence=0.3, recommended_action="MONITOR")

    Add corresponding tests in tests/test_pipeline/test_detector.py for each rule covering the happy path (rule fires), the negative path (rule does not fire), and edge cases (DET-01 private IP skip, DET-03 false-positive gate, DET-07 repeat suppression).
  </action>
  <verify>
    <automated>cd "C:/Users/alame/Desktop/network Lab project" && python -m pytest tests/test_pipeline/test_detector.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpg/pipeline/detector.py contains "def rule_det01_unknown_domain("
    - pnpg/pipeline/detector.py contains "def rule_det02_rate_spike("
    - pnpg/pipeline/detector.py contains "def rule_det03_unusual_port("
    - pnpg/pipeline/detector.py contains "def rule_det04_unknown_process("
    - pnpg/pipeline/detector.py contains "def rule_det05_blocklisted("
    - pnpg/pipeline/detector.py contains "def rule_det06_tor_exit_node("
    - pnpg/pipeline/detector.py contains "def rule_det07_new_destination("
    - pnpg/pipeline/detector.py contains "is_private" (DET-01 private IP skip)
    - pnpg/pipeline/detector.py contains "connection_rate_threshold_per_min" (DET-02 config read)
    - pnpg/pipeline/detector.py contains "known_processes" or "is_unknown" (DET-03 compound guard)
    - tests/test_pipeline/test_detector.py contains "test_det01" or "det01"
    - tests/test_pipeline/test_detector.py contains "test_det02" or "det02"
    - tests/test_pipeline/test_detector.py contains "test_det03" or "det03"
    - tests/test_pipeline/test_detector.py contains "test_det04" or "det04"
    - tests/test_pipeline/test_detector.py contains "test_det05" or "det05"
    - tests/test_pipeline/test_detector.py contains "test_det06" or "det06"
    - tests/test_pipeline/test_detector.py contains "test_det07" or "det07"
    - pytest tests/test_pipeline/test_detector.py -x -q exits 0
    - pytest tests/ -x -q exits 0 (full suite still green)
  </acceptance_criteria>
  <done>All 7 detection rule functions implemented with compound guards (DET-03), private IP skip (DET-01), config-driven thresholds (DET-08), and all unit tests passing. Full test suite still green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_pipeline/test_detector.py -x -q` — all detection rule tests pass
- `pytest tests/ -x -q` — full suite still green (no regressions)
- `grep -c "def rule_det0" pnpg/pipeline/detector.py` returns 7
- `grep "connection_rate_threshold_per_min" pnpg/config.py` shows the key exists
- `python -c "from pnpg.pipeline.detector import DetectorState, detect_event, load_tor_exit_nodes; print('imports ok')"` succeeds
</verification>

<success_criteria>
- DetectorState dataclass exists with 7 fields
- 7 rule functions (DET-01 to DET-07) each return alert dict or None
- _make_alert produces all 12 DET-10 fields
- TOR exit node loader works (happy path + missing file)
- DET-01 skips private IPs
- DET-02 uses sliding window with config threshold
- DET-03 has compound guard (unknown process OR rate spike)
- DET-08 satisfied: all thresholds from config dict
- All tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-detection-engine/04-01-SUMMARY.md`
</output>
