---
phase: 01-capture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config.yaml
  - pytest.ini
  - pnpg/__init__.py
  - pnpg/config.py
  - pnpg/prereqs.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_prereqs.py
  - tests/test_config.py
autonomous: true
requirements: [CAP-02, CAP-03, CONFIG-01, CONFIG-02, CONFIG-03, SYS-01]
user_setup:
  - service: npcap
    why: "Windows packet capture driver required by Scapy"
    env_vars: []
    dashboard_config:
      - task: "Install Npcap from https://npcap.com with 'WinPcap API-compatible mode' enabled"
        location: "https://npcap.com — download installer"

must_haves:
  truths:
    - "Running without Npcap installed prints a clear error and exits with code 1 before any sniffing"
    - "Running without administrator privileges prints a clear error and exits with code 1 before any sniffing"
    - "Missing config.yaml uses all default values without crashing"
    - "Invalid config.yaml keys are warned and ignored; valid keys override defaults"
    - "Critical errors in async tasks are logged without terminating the event loop"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned Python dependencies"
      contains: "scapy>=2.7.0"
    - path: "pnpg/config.py"
      provides: "Config loader with defaults and validation"
      exports: ["load_config", "DEFAULT_CONFIG"]
    - path: "pnpg/prereqs.py"
      provides: "Npcap and admin privilege checks"
      exports: ["check_npcap", "check_admin"]
    - path: "config.yaml"
      provides: "Default runtime configuration"
      contains: "queue_size"
    - path: "pytest.ini"
      provides: "Test framework configuration"
      contains: "asyncio_mode"
    - path: "tests/test_prereqs.py"
      provides: "Tests for CAP-02 and CAP-03"
      contains: "test_npcap_check_exits"
    - path: "tests/test_config.py"
      provides: "Tests for CONFIG-01 and CONFIG-02"
      contains: "test_defaults"
  key_links:
    - from: "pnpg/prereqs.py"
      to: "sys.exit(1)"
      via: "check_npcap() and check_admin() exit on failure"
      pattern: "sys\\.exit\\(1\\)"
    - from: "pnpg/config.py"
      to: "config.yaml"
      via: "yaml.safe_load reads user config, merges with DEFAULT_CONFIG"
      pattern: "yaml\\.safe_load"
---

<objective>
Set up the project skeleton, install dependencies, create the config loader and startup prerequisite checks (Npcap detection and admin privilege verification), and establish the test infrastructure with Wave 0 test stubs for this plan's requirements.

Purpose: Everything in Phase 1 depends on config being loadable and prerequisites being validated before any sniffing starts. This plan establishes the cold-start gate: load config -> verify Npcap -> verify admin.

Output: requirements.txt, pnpg/ package with config.py and prereqs.py, config.yaml with documented defaults, pytest.ini, and passing test suite for CAP-02, CAP-03, CONFIG-01, CONFIG-02.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-capture-foundation/01-RESEARCH.md
@.planning/phases/01-capture-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project skeleton, dependencies, test infrastructure, and Wave 0 test stubs</name>
  <files>
    requirements.txt,
    pytest.ini,
    pnpg/__init__.py,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_prereqs.py,
    tests/test_config.py
  </files>
  <read_first>
    .planning/phases/01-capture-foundation/01-RESEARCH.md (Standard Stack section for exact version pins),
    .planning/phases/01-capture-foundation/01-VALIDATION.md (Wave 0 Requirements section for test file list)
  </read_first>
  <behavior>
    - test_npcap_check_exits: When Npcap directory does not exist (mocked), check_npcap() calls sys.exit(1)
    - test_npcap_check_passes: When Npcap directory exists (mocked), check_npcap() returns None
    - test_admin_check_exits: When IsUserAnAdmin returns 0 (mocked), check_admin() calls sys.exit(1)
    - test_admin_check_passes: When IsUserAnAdmin returns 1 (mocked), check_admin() returns None
    - test_defaults: When config.yaml does not exist, load_config() returns dict with all keys from DEFAULT_CONFIG
    - test_config_override: When config.yaml has queue_size: 100, load_config() returns queue_size=100 with other keys at defaults
    - test_unknown_key_ignored: When config.yaml has unknown_key: 42, load_config() does not include unknown_key and logs warning
    - test_invalid_yaml: When config.yaml contains invalid YAML, load_config() returns all defaults and logs warning
  </behavior>
  <action>
    1. Create `requirements.txt` with exact minimum pins from RESEARCH.md:
       ```
       scapy>=2.7.0
       fastapi>=0.115.0
       uvicorn[standard]>=0.32.0
       psutil>=6.0.0
       pyyaml>=6.0.0
       pytest>=8.0.0
       pytest-asyncio>=0.23.0
       ```

    2. Create `pytest.ini`:
       ```ini
       [pytest]
       asyncio_mode = auto
       testpaths = tests
       ```

    3. Create `pnpg/__init__.py` as empty file (makes pnpg a package).

    4. Create `tests/__init__.py` as empty file.

    5. Create `tests/conftest.py` with shared fixtures:
       - `tmp_config_path(tmp_path)` fixture: returns a Path for a temporary config.yaml
       - `mock_npcap_missing()` fixture: patches `os.path.isdir` to return False for Npcap path and `winreg.OpenKey` to raise FileNotFoundError
       - `mock_npcap_present()` fixture: patches `os.path.isdir` to return True for the Npcap System32 path
       - `mock_admin()` fixture: patches `ctypes.windll.shell32.IsUserAnAdmin` to return 1
       - `mock_not_admin()` fixture: patches `ctypes.windll.shell32.IsUserAnAdmin` to return 0

    6. Create `tests/test_prereqs.py` with RED tests (these will fail until Task 2 implements prereqs.py):
       - `test_npcap_check_exits(mock_npcap_missing)`: call `check_npcap()`, assert `SystemExit` with code 1
       - `test_npcap_check_passes(mock_npcap_present)`: call `check_npcap()`, assert no exception
       - `test_admin_check_exits(mock_not_admin)`: call `check_admin()`, assert `SystemExit` with code 1
       - `test_admin_check_passes(mock_admin)`: call `check_admin()`, assert no exception

    7. Create `tests/test_config.py` with RED tests:
       - `test_defaults(tmp_path)`: call `load_config(str(tmp_path / "nonexistent.yaml"))`, assert returned dict has all keys from DEFAULT_CONFIG with correct default values. Assert `queue_size == 500`, `debug_mode == False`, `interface is None`, `poll_interval_ms == 200`.
       - `test_config_override(tmp_path)`: write `queue_size: 100` to tmp yaml, call `load_config(path)`, assert `queue_size == 100` and `debug_mode == False` (default preserved).
       - `test_unknown_key_ignored(tmp_path, caplog)`: write `bogus_key: 42` to tmp yaml, call `load_config(path)`, assert `"bogus_key"` not in returned dict and `"Unknown config key"` in caplog.text.
       - `test_invalid_yaml(tmp_path, caplog)`: write `{{{invalid` to tmp yaml, call `load_config(path)`, assert returned dict equals DEFAULT_CONFIG and `"parse error"` in caplog.text.

    8. Run `pip install -r requirements.txt` to install all dependencies.

    9. Run `python -m pytest tests/ -x -q` — all tests should FAIL (RED) because pnpg/config.py and pnpg/prereqs.py do not exist yet.
  </action>
  <verify>
    <automated>python -m pytest tests/test_prereqs.py tests/test_config.py --co -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains `scapy>=2.7.0` and `pyyaml>=6.0.0` and `pytest>=8.0.0`
    - pytest.ini contains `asyncio_mode = auto`
    - pnpg/__init__.py exists (can be empty)
    - tests/test_prereqs.py contains `def test_npcap_check_exits` and `def test_admin_check_exits`
    - tests/test_config.py contains `def test_defaults` and `def test_config_override` and `def test_unknown_key_ignored`
    - tests/conftest.py contains `mock_npcap_missing` and `mock_admin`
    - `python -m pytest tests/ --co -q` lists at least 8 test items
    - `pip install -r requirements.txt` exits 0
  </acceptance_criteria>
  <done>All test stubs are collected by pytest (--co succeeds), all 8 tests are RED (import errors since implementation does not exist), dependencies installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prereqs.py, config.py, config.yaml — make all tests GREEN</name>
  <files>
    pnpg/prereqs.py,
    pnpg/config.py,
    config.yaml
  </files>
  <read_first>
    tests/test_prereqs.py (the tests that must pass),
    tests/test_config.py (the tests that must pass),
    tests/conftest.py (fixtures used by the tests),
    .planning/phases/01-capture-foundation/01-RESEARCH.md (Pattern 5: Npcap Detection, Pattern 6: Admin Check, Pattern 7: Config Loader)
  </read_first>
  <behavior>
    Tests from Task 1 define the behavior. Implementation must make them pass.
  </behavior>
  <action>
    1. Create `pnpg/prereqs.py` implementing two functions per RESEARCH.md Pattern 5 and Pattern 6:

       `check_npcap() -> None` (CAP-02):
       - Check `os.path.isdir(os.path.join(os.environ.get("WINDIR", "C:\\Windows"), "System32", "Npcap"))`
       - If not found, try registry keys `SOFTWARE\Npcap` and `SYSTEM\CurrentControlSet\Services\npcap` via `winreg.OpenKey(winreg.HKEY_LOCAL_MACHINE, ...)`
       - If neither check passes: `print("ERROR: Npcap is not installed.", file=sys.stderr)` then `print("Install Npcap from https://npcap.com (enable WinPcap API-compatible mode).", file=sys.stderr)` then `sys.exit(1)`

       `check_admin() -> None` (CAP-03):
       - Call `ctypes.windll.shell32.IsUserAnAdmin()` inside try/except
       - If returns falsy: `print("ERROR: PNPG requires administrator privileges.", file=sys.stderr)` then `print("Right-click your terminal and select 'Run as administrator'.", file=sys.stderr)` then `sys.exit(1)`

    2. Create `pnpg/config.py` implementing per RESEARCH.md Pattern 7:

       `DEFAULT_CONFIG` dict with these exact keys and values:
       ```python
       DEFAULT_CONFIG = {
           "queue_size": 500,
           "poll_interval_ms": 200,
           "alert_rate_limit_per_sec": 1,
           "port_allowlist": [80, 443, 53, 123, 5353, 8080, 8443],
           "log_rotation_size_mb": 50,
           "debug_mode": False,
           "interface": None,
           "connection_rate_threshold": 50,
           "dns_cache_size": 1000,
           "dns_cache_ttl_sec": 300,
           "proc_cache_ttl_sec": 2,
           "log_dir": "logs",
       }
       ```

       `load_config(path: str = "config.yaml") -> dict` (CONFIG-01, CONFIG-02):
       - Try `open(path, "r")` and `yaml.safe_load(f)`
       - `FileNotFoundError`: log INFO "config.yaml not found -- using all defaults"
       - `yaml.YAMLError`: log WARNING with parse error message, use all defaults
       - Create new dict from `DEFAULT_CONFIG` (immutable — do NOT mutate DEFAULT_CONFIG)
       - For each key in user_config: if key in DEFAULT_CONFIG, override; else `logging.warning("Unknown config key ignored: %s", key)`
       - Return merged config dict

    3. Create `config.yaml` with all keys documented:
       ```yaml
       # PNPG Runtime Configuration
       # All values shown are defaults — uncomment and change as needed.

       # queue_size: 500          # Max packets in asyncio queue (CAP-05)
       # poll_interval_ms: 200    # psutil polling interval in ms (PROC-02)
       # alert_rate_limit_per_sec: 1  # Max alerts per rule per second (DET-06)
       # port_allowlist: [80, 443, 53, 123, 5353, 8080, 8443]  # Safe ports (DET-03)
       # log_rotation_size_mb: 50 # Max log file size before rotation (STORE-04)
       # debug_mode: false        # Print pipeline events to console (TEST-01)
       # interface: null           # null = auto-select; or specify e.g. "Wi-Fi" (CAP-09)
       # connection_rate_threshold: 50  # Connections/sec trigger (DET-02)
       # dns_cache_size: 1000     # Max DNS cache entries (DNS-06)
       # dns_cache_ttl_sec: 300   # DNS cache TTL in seconds (DNS-04)
       # proc_cache_ttl_sec: 2    # Process cache TTL in seconds (PROC-06)
       # log_dir: logs            # Directory for NDJSON log files
       ```

    4. Run `python -m pytest tests/test_prereqs.py tests/test_config.py -v` — all tests must be GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_prereqs.py tests/test_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - pnpg/prereqs.py contains `def check_npcap()` and `def check_admin()`
    - pnpg/prereqs.py contains `sys.exit(1)` (at least twice — once per check function)
    - pnpg/prereqs.py contains `IsUserAnAdmin` string
    - pnpg/prereqs.py contains `System32` and `Npcap` strings for filesystem check
    - pnpg/config.py contains `DEFAULT_CONFIG` dict with `"queue_size": 500`
    - pnpg/config.py contains `def load_config(`
    - pnpg/config.py contains `yaml.safe_load`
    - pnpg/config.py contains `logging.warning("Unknown config key ignored`
    - config.yaml contains `queue_size` and `debug_mode` as comments
    - `python -m pytest tests/test_prereqs.py tests/test_config.py -v` exits 0 with all tests passed
  </acceptance_criteria>
  <done>All 8 tests pass GREEN. check_npcap() exits on missing Npcap, check_admin() exits on non-admin, load_config() returns defaults when config.yaml missing, overrides when present, warns on unknown keys, handles invalid YAML.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_prereqs.py tests/test_config.py -v` — all tests pass
- `python -c "from pnpg.prereqs import check_npcap, check_admin"` — imports succeed
- `python -c "from pnpg.config import load_config, DEFAULT_CONFIG; c = load_config(); print(c['queue_size'])"` — prints 500
- `pip install -r requirements.txt` exits 0
</verification>

<success_criteria>
- requirements.txt exists with all 7 dependency pins
- pnpg/ package exists with config.py and prereqs.py
- config.yaml exists with all documented defaults
- pytest.ini exists with asyncio_mode = auto
- All tests in test_prereqs.py and test_config.py pass
- No hardcoded secrets in any file
</success_criteria>

<output>
After completion, create `.planning/phases/01-capture-foundation/01-01-SUMMARY.md`
</output>
