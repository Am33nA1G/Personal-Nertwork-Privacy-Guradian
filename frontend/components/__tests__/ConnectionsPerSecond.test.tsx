// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { render } from '@testing-library/react';

describe('ConnectionsPerSecond', () => {
  it.todo('renders a LineChart');
  it.todo('trims entries older than 60s from buffer');
  it.todo('does not animate (isAnimationActive false)');
});
